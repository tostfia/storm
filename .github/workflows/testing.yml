name: Build and Test BookKeeper

on:
  push:
    branches: [ master, testing ]
  pull_request:
    branches: [ master, testing ]

env:
  RUN_TESTS: true   # metti false se vuoi disabilitarli temporaneamente

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Cache Maven packages
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven

      - name: Build and Install (No Tests)
        # Popola la cartella target e la cache .m2 senza eseguire test
        run: mvn -B clean install -DskipTests

      - name: Run Unit Tests (Surefire)
        if: env.RUN_TESTS == 'true'
        # Esegue i test di unità associandoli a Maven Surefire
        run: mvn -B test

      - name: Run Integration Tests (Failsafe)
        if: env.RUN_TESTS == 'true'
        # Esegue i test di integrazione associandoli a Maven Failsafe
        # Si usa 'verify' per includere le fasi di post-integrazione
        run: mvn -B verify -DskipUnitTests

      - name: Tests disabled
        if: env.RUN_TESTS != 'true'
        run: echo "Tests are disabled"